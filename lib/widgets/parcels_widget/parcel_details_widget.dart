import 'package:flutter/material.dart';

class ParcelDetailsWidget extends StatelessWidget {
  const ParcelDetailsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
